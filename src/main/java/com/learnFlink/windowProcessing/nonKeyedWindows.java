package com.learnFlink.windowProcessing;

public class nonKeyedWindows {
    // use .windowAll instead of .window
    // other things are the same
    // or use key selector (data -> true) to put all data in one key
}
